<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linegrillpresent.studybuddy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/sb_v3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RegisterNewGroup" />
        <activity android:name=".RegisterNewCourse" />
        <activity
            android:name=".ShowGroupInfoActivity"
           />
        <activity android:name=".WaitActivity" />
        <activity android:name=".ShowClassInfoActivity" />
        <activity android:name=".MycourseActivity" />
        <activity android:name=".MygroupActivity" />
        <activity android:name=".MyprofileActivity" />
        <activity android:name=".WelcomePage" />
        <activity
            android:name=".BookingActivity"
            android:label="@string/title_activity_booking"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>